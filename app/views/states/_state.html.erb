<div id="<%= dom_id state%>">
  <p>
    <strong>Name:</strong>
    <%= state.name %>
  </p>
  <% if current_user.admin %>
    <p>
      <strong>User:</strong>
      <%= User.find_by(id: state.user_id).email %>  
    </p>
    <p>
      <strong>Global:</strong>
      <%= state.global %>
    </p>
  <% end %>
</div>
