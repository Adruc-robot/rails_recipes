<%= form_with(model: state) do |form| %>
  <% if state.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(state.errors.count, "error") %> prohibited this state from being saved:</h2>

      <ul>
        <% state.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.number_field :user_id, id: :state_user_id, value: current_user.id, type: :hidden %>
  </div>

  <% if current_user.admin == "T" %>
    <div>
      <%= form.label :global %> <br />
      <%= form.select :global, [["True","T"],["False","F"]] %>
    </div>
  <% end %>
  <div>
    <%= form.submit %>
  </div>
<% end %>
